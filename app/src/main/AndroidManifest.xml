<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tuempresa.proyecto_01_11_25">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Habitus+"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Proyecto_01_11_25">

        <!-- Pantalla principal (Splash) -->
        <activity
            android:name=".ui.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras Activities -->
        <activity android:name=".ui.DashboardActivity" />
        <activity android:name=".ui.CreateHabitActivity" />
        <activity android:name=".ui.HabitListActivity" />
        <activity android:name=".ui.HabitDetailActivity" />

    </application>

</manifest>
